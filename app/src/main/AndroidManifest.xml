<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs.uga.edu.roommateshoppingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoommateShoppingApp">
        <activity android:name=".ListPop"></activity>
        <activity android:name=".Expenses" />
        <activity android:name=".List" />
        <activity android:name=".Groups" />
        <activity android:name=".Home" />
        <activity android:name=".FirebaseTestingActivity" />
        <activity android:name=".SplashActivity2" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>